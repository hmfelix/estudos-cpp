#include <iostream>

int main() {
    
    // comparacoes:
    bool t = 5;
    if(t == true) {
        printf("Deu certo!");
    };

    // para uma linha de codigo nao precisa das chaves {}
    if (t) printf("Opa"); // de qq forma pode ou nao ter espaco antes do parentese
    // mas: para fins de debug eh melhor pelo menos separar e jogar pra linha de baixo

}
